class WASession{

    constructor(code, status, role){
        this.code = code == "000000" ? undefined : code
        this.status = status
        if (role == undefined) this.role = "default"
    }
}

module.exports = {WASession};